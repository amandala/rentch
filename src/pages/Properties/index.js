import React from "react";
import PropertyLinkList from "../../components/PropertyLinkList";

const Properties = ({ properties }) => {
  return <PropertyLinkList properties={properties} />;
};

export default Properties;
